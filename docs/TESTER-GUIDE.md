# DSH Ren'Py 开发工作台 — 测试用户功能手册

> 本文档面向**测试用户**：第一部分「功能详解」逐功能讲清**这个功能是什么、怎么用、完整行为、边界**；
> 第二部分「操作与测试」给出按步骤的验证方法。两者配套：先读懂功能，再动手测。
>
> 配套阅读：部署见 `DEPLOY.md`，界面规范见 `skills/workbench-ui.md`。

---

## 0. 环境与启动

### 0.1 前置条件（已配置好）

| 项目 | 位置/说明 |
|---|---|
| DSH | 本机已安装并运行（Web GUI `http://127.0.0.1:3080`） |
| Ren'Py SDK | 工作区 `renpy-8.5.3-sdk/`（锁定 8.5.x） |
| DSH Ren'Py 开发工作台 | 已挂载为 agent preset「RenPy Dev」，新会话可选 |
| 示例项目 | `demo-script/`（测试用）；SDK 自带 `the_question/`、`tutorial/` |

### 0.2 打开开发面板

1. 在 DSH Web GUI 开一个新会话（或切换 preset 到 **RenPy Dev**）
2. 在会话的对话页签上方找到 **"Ren'Py"** 页签并点击 → 出现开发面板
3. 顶部输入框输入你的 Ren'Py 项目路径（含 `game/` 目录的项目根，如 `D:\my-game`），点 **⟳ 加载**

### 0.3 生效规则（重要）

- **只改了编辑器 UI（client 侧）** → 浏览器**刷新页面**即生效
- **改了后端服务（host 侧）** → 需要**重启 dsh** 才生效（文档中凡是提到 host 改动的功能都如此）
- **改了 agent preset / skill** → 重启 dsh 后**新会话**生效
- 本插件对 DSH 宿主的两处视觉调整（隐藏原生输入框、侧栏 logo 品牌色）为**运行时注入**，刷新/重开即生效，不影响部署

### 0.4 快速开始（5 分钟上手）

| 步骤 | 操作 | 预期 |
|---|---|---|
| 1 | 在 DSH Web 界面（`http://127.0.0.1:3080`）新建会话，preset 选「RenPy Dev」 | 出现新会话 |
| 2 | 会话上方找到 **「Ren'Py」页签**并点击 | 打开开发面板 |
| 3 | 顶部输入框填入项目路径（含 `game/` 目录的项目根，如 `D:\my-game`），回车或点 **⟳** | 左侧出现文件树、中间出现编辑器 |
| 4 | 点开 `game/script.rpy` | 编辑器打开，代码带彩色语法高亮 |
| 5 | 顶栏点 **⚠ 检查** | 下方出现「lint 通过」 |

> **没有项目？** 在对话里对 AI 说「帮我新建一个测试项目」即可（AI 会用 `renpy_scaffold` 生成）。

---

## 1. 界面总览

### 功能详解

- **用途**：把 DSH 会话页扩展成完整 Ren'Py 开发工作台——四区布局（控件栏/顶栏/编辑器区/侧栏），全部面板可停靠、拖拽、浮动、最大化。
- **布局**（自上而下、自左而右）：
  - **控件栏**（最左，46px 收起 / 172px 展开）：编辑工具 → 调试面板 → 视图控件 → 面板控件 → **⚙ 设置（固定底部）** → 收缩按钮
  - **顶栏工具栏**：项目输入 + 工作范围 + 运行/停止合一 + 检查/测试/刷新/保存/截图/对话
  - **中央**：多文件标签页编辑器（语法高亮 + 行号 + 查找栏 + lint 下划线）
  - **底部面板区**：操作日志 / lint 结果 / 测试报告（可拖高）
  - **右侧栏**：对话面板（默认）及所有调试面板（点击控件栏停靠于此）
- **图标**：统一 codicon 线框 SVG（悬停显示中文 tooltip），颜色跟随主题；`«/»` 按钮收起/展开控件栏。

```
┌─────────┬──────────────────────────────────────────────┬─────────┐
│ 控件栏   │  顶栏工具栏                                    │ 右侧栏   │
│ (46px)  │  项目路径[输入] ⟳ 🎯工作范围 ⚠检查 🧪测试       │ 对话面板 │
│         │  ⟳刷新 💾保存 📷截图 💬对话 ▶运行/■停止         │ (默认)   │
│ 编辑工具 ├──────────────────────────────────────────────┤          │
│ Aa 预览  │  编辑器（多文件标签页 + 语法高亮 + 查找栏）      │          │
│ ⇄ 对照  │                                               │          │
│ 📖 学习  │                                               │          │
│ 🎨 GUI   │                                               │          │
│ ──────  │                                               │          │
│ 调试面板 ├──────────────────────────────────────────────┤          │
│ 🗺 路线图│  底部面板区（日志 / lint 结果 / 测试报告）      │          │
│ 🎬 画面  │                                               │          │
│ 📊 变量  │                                               │          │
│ 🐞 报错  │                                               │          │
│ ✅ 诊断  │                                               │          │
│ ──────  │                                               │          │
│ 视图     │                                               │          │
│ 📄 文件  │                                               │          │
│ 🧭 导航  │                                               │          │
│ 🖼 素材  │                                               │          │
│ ✎ 修改  │                                               │          │
│ ──────  │                                               │          │
│ 📋 日志  │                                               │          │
│ 📜 历史  │                                               │          │
│ ⚙ 设置  │  （⚙ 设置固定在底部）                          │          │
│ « 收起   │                                               │          │
└─────────┴──────────────────────────────────────────────┴─────────┘
```

---

## 2. 顶栏工具栏（功能详解）

### 2.1 项目输入框

- **用途**：指定要开发的 Ren'Py 项目（含 `game/` 目录的项目根）。
- **完整行为**：回车或失焦提交 → 加载文件树/资源/索引；项目路径**持久化**（localStorage，重开面板自动恢复）；提交新项目会清空旧标签页重新加载。
- **边界**：空路径忽略；路径不存在时加载结果为空的文件树（可点 ⟳ 刷新重试）。

### 2.2 ⟳ 默认工程

- **用途**：一键切换到 host 配置的默认项目（`renpy.config.json` 的 `defaultProject`）。
- **行为**：点击 → 自动填入并加载；未配置时日志提示「host 未配置默认工程」。

### 2.3 🎯 工作范围（安全约束）

- **用途**：锁定编辑范围——区域内可改，区域外**只读**，AI 越界会先询问（防止 AI 乱改无关代码）。
- **完整行为**：① 编辑器**选中几行**（或光标停在某行）→ 点 🎯 → 生成绿色高亮区域；② 设置后向对话注入【工作区域】约束消息（AI 的修改被限定在范围内）；③ 区域外**手动编辑被拦截**（输入无效并提示）；④ 按钮旁出现「✖ 清除」，点击解除。
- **边界**：范围以当前文件为准（切换文件后原范围仅对原文件生效）；清除前 AI 始终受约束。
- **相关功能**：写守卫（§9）是另一层保护；学习注释（📖）会尊重工作范围（只注释区域内行）。

### 2.4 ▶ 运行 / ■ 停止（合一按钮）

- **用途**：启动/停止真实游戏进程（带**调试桥接**自动注入，供路线图/画面/变量面板使用）。
- **完整行为**：未运行 → 显示「▶ 运行游戏」（蓝色实底），点击启动（自动注入 `_debug_bridge.rpy`）；运行中 → 显示「■ 停止游戏」（红色描边），点击停止。状态由后端 **2 秒轮询 + 点击乐观更新**双驱动——**游戏自行退出（关窗口/崩溃）后按钮自动恢复 ▶**。
- **边界**：同一项目同时只运行一个实例（再次运行会先停旧的）。
- **相关功能**：📷 截图、🗺 路线图、🎬 实时画面、📊 运行时变量。

### 2.5 ⚠ 检查（lint）

- **用途**：Ren'Py 引擎级语法检查（权威）。
- **完整行为**：点击 → 调用 SDK lint → 下方 lint 面板显示结果；**有错误的行在编辑器内出现红色下划线**；点击 lint 条目跳转对应行。
- **边界**：lint 报错基于引擎解析；与静态诊断（✅）的关系是「lint 权威、✅ 秒级快速初检」。

### 2.6 🧪 测试（自动化测试）

- **用途**：运行项目内 `testsuite`（rpytest），无 testsuite 时提示。
- **行为**：点击 → 运行测试 → 报告条显示 ✓/✗ 与通过数，「详情」可展开完整报告。

### 2.7 ⟳ 刷新

- **用途**：重新扫描 `game/` 文件列表（项目外新增文件后使用）。
- **行为**：重扫 .rpy 列表与素材树；已打开的标签页保留。

### 2.8 💾 保存

- **用途**：保存当前文件（快捷键 `Ctrl+S`）。
- **完整行为**：保存成功 → 状态栏 ✓；**每次保存自动备份**（§6 历史）；保存前触发**写守卫**校验（§9，结构错误会被拦截）。
- **边界**：文件在外部被改动且本地有未保存修改时，出现冲突提示（本地保留，关标签重开可加载外部版本）。

### 2.9 📷 截图

- **用途**：整屏截图（游戏窗口反馈），供人类和 AI 查看游戏当前画面。
- **行为**：点击 → 截取当前屏幕 → 保存到 `.renpy-user/screenshots/`，对话/面板可查看。

### 2.10 💬 对话

- **用途**：打开/聚焦对话面板（停靠右侧栏）。
- **行为**：点击 → 右侧栏出现对话面板（消息流 + 检查点时间线 + 轨迹）。

### 操作与测试

| 按钮 | 操作 | 预期 |
|---|---|---|
| 项目框 | 输入路径回车 | 文件树/编辑器加载 |
| ⟳ | 点击 | 填入默认工程 |
| 🎯 | 选中几行后点击 | 绿色高亮区域；区域外打字被拦截 |
| ▶/■ | 点击运行 → 再点 | 游戏窗口弹出 → 按钮变红 ■ → 再点停止恢复 ▶ |
| ⚠ | 故意删冒号后点击 | 报错 + 红线定位 |
| 🧪 | demo 项目点击 | 报告全通过 |
| ⟳ | 新建 .rpy 后点击 | 新文件出现 |
| 💾 | Ctrl+S | 状态栏 ✓ |
| 📷 | 运行中点击 | 生成 PNG |
| 💬 | 点击 | 右侧对话面板出现 |

---

## 3. 控件栏（左侧竖条，功能详解）

> 悬停每个图标显示右侧 tooltip；展开状态图标下显示文字。分组自上而下。

### 3.1 编辑工具（作用于编辑器）

| 图标 | 功能 | 功能详解 |
|---|---|---|
| **Aa** | 样式预览 | **用途**：所见即所得预览对白文本样式。**行为**：打开含 say 文本的文件 → 点击 → 对白**直接按样式渲染**：`{b}`粗体/`{i}`斜体/`{u}`下划线/`{s}`删除线/`{color}`颜色/`{size}`字号/`{font}`字体/`{alpha}`透明度/`{kern}`字距真实显示；`[变量]`插值、`{cps}`速度等运行时语义以**黄/蓝/红底色标记** + 悬停提示；行高自动放大；无法精确渲染时出现「降级提示条」。**预览模式下点击 say 行** → 右下角浮窗**打字动画**逐字播放（速度读项目配置 `slow_cps`），▶ 重播、✕ 关闭。**边界**：`{font}` 需字体文件存在于项目（存在显示加载中、不存在提示移除）；预览是渲染层，不修改文件。 |
| **⇄** | Python 对照 | **用途**：理解 Ren'Py 语句对应的 Python 实现。**行为**：光标停在语句行（`show`/`jump`/`call`/`menu`/`if` 等）→ 点击 → 右下角浮窗显示 Python 等价（如 `renpy.show(...)`）+ 语义说明；无法识别则日志提示。 |
| **📖** | 学习注释 | **用途**：AI 逐行讲解代码（生成教学注释）。**行为**：点击 → 确认弹窗（显示范围：工作区域内 Lx-y 或整个文件 + 行数 + **消耗 AI 资源提示**）→ 确认后 AI 为范围内语句行生成 `# 📖 学习: <skill>（L<行>）` 注释块写入文件；结果条显示「已写入 N 条」+「清除全部」。**边界**：消耗模型 token；批量一次调用（90s 超时保护）；注释是合法 Python 注释，不影响 lint。**相关**：尊重工作范围（只注释区域内行）。 |
| **🎨** | GUI 主题 | **用途**：可视化定制 `gui.rpy`（分辨率/主题色/字号）。**行为**：打开含 gui.rpy 的项目 → 点击 → 面板显示 `gui.init` 分辨率、7 个主题色（颜色选择器）、字号等 → 修改后「保存到 gui.rpy」写回（编辑器标签同步刷新）；「查看源码」直接打开 gui.rpy。**边界**：无 gui.rpy 的项目显示默认值，保存会新建文件。 |

### 3.2 调试面板（点击后停靠右侧栏）

| 图标 | 面板 | 功能详解 |
|---|---|---|
| 🗺 | 分支路线图 | **用途**：项目流程可视化（label 状态机）。**行为**：解析全部 .rpy 的 label 与跳转关系 → 节点连线图（绿色=结局、蓝色=起点、紫色=选择、红色=死路、灰色=孤立、黄色=循环）；点击节点 → 运行中跳转游戏到该 label；2s 轮询游戏当前进度（高亮当前节点）。**边界**：需运行游戏才可跳转；只在调试面板可见时轮询。 |
| 🎬 | 实时画面 | **用途**：游戏画面实时反馈 + 远程操作。**行为**：显示游戏内截图（`game/_shot.png`，由桥接注入定时截取）；可**点击画面模拟游戏点击**、推进/回滚（桥接指令）。**边界**：需运行游戏；截取频率受桥接限频（0.5s）。 |
| 📊 | 运行时变量 | **用途**：监控游戏运行时变量。**行为**：桥接回报变量快照 → 列表显示实时值，**变化高亮闪烁 1.5s**；点击变量名可跳转编辑器定义处。 |
| 🐞 | 报错诊断 | **用途**：游戏崩溃后结构化查看错误。**行为**：读取项目根目录 `traceback.txt`/`log.txt`/`errors.txt`（Ren'Py 崩溃自动生成）→ 解析为：崩溃位置（While 运行中…）、**根因**（最深 game/ 帧）、完整栈帧列表、lint 错误、log 内嵌报错段；点击任意 file:line **跳转编辑器**。**边界**：无错误文件时显示空状态引导（运行崩溃或 lint 失败后自动生成）。 |
| ✅ | 静态诊断 | **用途**：全项目**引用完整性**秒级扫描（与 lint 互补：lint 权威、本扫描快速初检）。**行为**：扫描全部 .rpy → 五类问题分组显示：**无效跳转**（jump/call 目标不存在）、**未定义 screen**（show/call screen、use）、**未定义角色**（say 的说话人）、**缺失资源**（show/scene 图片、play 音频、`{font=}` 字体）、**不可达 label**（start 无法到达）；error/warn/info 着色；点击条目跳转。**边界**：动态跳转（`jump expression`）、引擎内置对象已排除（零误报校准）；只报确定错误。 |

### 3.3 视图控件（停靠左侧栏）

| 图标 | 面板 | 功能详解 |
|---|---|---|
| 📄 | 项目文件 | 文件树：`game/` 下全部 .rpy，目录折叠/展开，点击打开（多标签页）。右上角 ⟳ 刷新、▾ 折叠全部。 |
| 🧭 | 导航 | 项目索引（引擎 dump + 轻量分析）：**标签/人物/转场/变量/字体** 五类 tab，点击条目跳转编辑器对应行。 |
| 🖼 | 项目素材 | 素材树：图片/音频/视频/字体分类，点击叶子**右下角弹出预览浮窗**（图片/音频可拖动）。 |
| ✎ | 基线更改 | 相对检查点基线的文件改动列表（与修改面板同源）。 |

### 3.4 面板控件

| 图标 | 功能 | 功能详解 |
|---|---|---|
| 📋 | 操作日志 | 停靠底部区：所有操作的时间线记录（加载/保存/lint/运行/工作范围等，带时间与结果）。 |
| 📜 | 保存历史 | **用途**：文件版本回滚。**行为**：点击 → 弹层列出当前文件全部备份版本（**每次保存自动备份**，时间倒序，含行数差异）→ 点版本右侧预览 → 「恢复」一键还原。**边界**：备份存 `.renpy-user/backups/`，不自动清理。 |
| **⚙** | **个性化设置** | **固定在控件栏底部**。全屏设置面板（Esc/✕ 关闭），完整详解见 §12。 |

底部 **« / »**：收起/展开控件栏（收起只显图标，hover 出说明）。

### 操作与测试

- Aa：打开含 `{b}`/`{color=#ff0000}`/`{size}`/`{font}` 的文本 → 预览 → 逐项确认渲染 → 点击行播打字动画
- ⇄：光标停 `show eileen happy at right` → 应显示 `renpy.show(...)`
- 📖：确认弹窗范围/行数/消耗提示 → 生成注释 → lint 通过 → 清除全部
- 🎨：改主题色保存 → gui.rpy 对应变量变化
- ✅：故意写 `jump no_such_label` → 扫描 → 报「无效跳转」；改回后消失
- 🐞：运行中写坏脚本制造崩溃 → 打开面板 → 根因定位 + 跳转
- 📜：保存 3 次 → 恢复最早版本 → 内容还原

---

## 4. 编辑器（功能详解）

### 4.1 语法高亮（含 Python 块）

- **RPY 语句**：语句关键字蓝、名称黄、字符串橙、注释绿、数字青（Dark+ 风格，亮色主题对应 Light+）。
- **Python 块**（对齐 VSCode Dark+/Light+ 四级配色）：
  - 识别 `init python:` / `init -10 python:` / `python early:` / `python hide:` / `rpy python:` 块内行、`$` 单行、`define`/`default` 等号右侧表达式
  - **关键词紫**（def/if/for/while/return/import/class/try/with/as/lambda/async…）
  - **内置类型青**（str/int/bool/list/dict/set/tuple/object…）
  - **内置函数蓝**（print/len/range/enumerate/map/filter/sorted…）
  - **函数调用黄**（`foo(` 这种自定义函数名）
- **边界**：`$` 单行强制按 Python 高亮；块内空行保持 Python 状态。

### 4.2 代码补全

- 输入关键字 → `Ctrl+Space` → 补全面板（**语句**/角色/资源/代码片段）；语句片段含注释说明（如 `menu` 展开选择菜单骨架）。

### 4.3 查找替换

- `Ctrl+F` → 查找栏：匹配**高亮**（含跨行）；`Enter`/`Shift+Enter` 上/下导航；替换单个/全部；`✕` 关闭。

### 4.4 括号与注释

- **括号匹配**：光标停在 `(`/`{`/`[` 附近 → 配对括号高亮（`Ctrl+Shift+\` 跳转）。
- **注释切换**：`Ctrl+/` 当前行注释/取消（写 `#`）。

### 4.5 未保存与外部冲突

- **未保存提示条**：修改后未保存 → 编辑器下方「未保存修改 +N 行 -M 行 [保存] [撤回修改]」（撤回 = 放弃未保存改动恢复到上次保存）。
- **外部修改冲突**：本地有未保存修改时文件被外部（AI/磁盘）改动 → 提示「外部修改冲突」，保留本地；关标签重开可加载外部版本；若本地无未保存修改则自动同步外部版本。

### 4.6 行号与参考线

- 行号模式（设置）：`on` / `relative`（当前行显示自身、其余显示距离）/ `off`。
- 缩进参考线（设置开关）、垂直参考线 rulers（设置数组列号）。

### 操作与测试

- 高亮：写 `init python:` 块含 def/if/str/print/函数调用 → 四色齐全；`$ x = 1`、`define e = Character(...)` 生效
- 补全：输入 `menu` + `Ctrl+Space` → 片段
- 查找：Ctrl+F 匹配/替换
- 未保存：改后不保存 → 提示条；外部改 → 冲突提示

---

## 5. 面板系统（功能详解）

- **标题栏**：最左 ⠿ **拖拽手柄** → 面板图标+名称 → 操作（刷新/折叠）→ **⛶ 最大化** → **✕ 移除**。
- **拖拽停靠**：按住标题栏拖动 → 拖到左/右/下区域**吸附**（高亮预览 + 插入位置指示线），或拖出成**浮动窗**；浮动窗可拖动、右下角**缩放**、Esc 关闭。
- **最大化**：⛶ 占满整个工作区（隐藏控件栏/侧栏/面板区），🗗 还原；最大化视图标题栏为「图标+标题+操作+✕ 关闭」。
- **布局持久化**：停靠区域/比例/面板列表保存到 localStorage（重开面板恢复）。
- **空状态**：面板移除后区域显示居中原型提示；可从控件栏随时重开。

### 操作与测试

- 拖拽「日志」面板到底部 → 吸附；拖出成浮动窗 → 缩放 → Esc 关闭
- ⛶ 最大化报错面板 → 占满 → 🗗 还原
- ✕ 移除「对话」→ 控件栏 💬 重新打开

---

## 6. 保存历史与回滚（功能详解）

- **机制**：**每次保存**（Ctrl+S / 💾）自动把旧版本备份到 `.renpy-user/backups/<project>/<file>/<时间戳>.bak`（含递增序号防同毫秒覆盖）。
- **入口**：控件栏 📜 保存历史（或编辑时相关入口）。
- **完整行为**：弹层列出该文件全部版本（时间倒序，含每版行数增减）→ 点版本**右侧预览内容** → 点「恢复」→ 文件还原为所选版本（可再次保存）。
- **边界**：只覆盖保存过的版本（未保存的改动不在历史里）；备份不自动清理，长期使用会累积。

### 操作与测试

保存 3 次（每次改不同内容）→ 📜 → 恢复最早版本 → 文件内容回到最初 → 再保存 → 新版本入列。

---

## 7. 检查点与修改面板（功能详解）

- **检查点**：**每轮 AI 对话**自动建立基线快照（`.renpy-user/checkpoints/`）；侧栏对话底部「📌 持久检查点」时间线可切到任意历史检查点查看/恢复。
- **修改面板**：AI 改过代码后，控件栏 **✎ 修改 (N)** 出现改动数徽标 → 点击打开：
  - 按文件列出 `+A -B` **行级 diff**；展开 hunk 显示起始行；点击**跳转编辑器**对应行
  - 编辑器左侧 **gutter 颜色标记**：绿=新增、蓝=修改、红=删除
  - 「全部通过」= 接受所有改动推进基线；「全部撤回」= 全部还原；每文件可单独「通过/撤回」
- **边界**：撤回以基线为准（AI 改的 + 你自己手动改的都在 diff 里，可整体/分文件还原）。

### 操作与测试

让 AI「给 script.rpy 加角色和一句对话」→ ✎ 修改出现 → diff 与 gutter 标记 → 「撤回」→ 文件还原 → 再「通过」→ 基线更新 → 检查点时间线新增条目。

---

## 8. 工作范围（功能详解）

- **用途**：锁定编辑范围防越界（人和 AI 都受限）。
- **完整行为**：选中行/光标行 → 🎯 → 绿色高亮；**区域外手动编辑被拦截**（输入无效 + 提示）；对话注入【工作区域】约束（AI 修改限定在区域内，越界先询问）；✖ 清除解除。
- **边界**：范围按文件记录；清除或重新设范围会再次注入约束消息；学习注释（📖）尊重范围。

### 操作与测试

锁 5-10 行 → 区域外打字被拒 → 让 AI 改文件（只动区域内）→ 清除 → 恢复正常。

---

## 9. 写守卫（功能详解）

- **用途**：保存前四层结构校验，防止 AI/手动写入破坏脚本结构。
- **校验项**：
  | 类型 | 示例 |
  |---|---|
  | 缩进错误 | tab/空格混用、label 后语句未缩进 |
  | 保留名 | 用 Python 关键字/Ren'Py 语句做 label、变量名（如 `label if:`） |
  | 重复 label | 文件内或跨文件重复定义 |
  | 对白括号 | `{ }` 与 `[ ]` 不配对（`{{`/`[[` 字面量除外） |
- **行为**：保存 .rpy 时自动校验 → 发现问题弹出「写守卫拦截了保存」+ 错误列表（行号/类型）→ 「取消（先修正）」回编辑器 / 「仍要保存（强制）」绕过。
- **边界**：只报**确定错误**（动态跳转、渲染期错误不判）；非 .rpy 文件不校验；强制保存会写入但结构错误仍在。

### 操作与测试

写 `label if:` 保存 → 被拦截 + 提示保留名 → 修正后保存通过 → 再试「仍要保存（强制）」→ 写入成功。

---

## 10. 运行与调试（功能详解）

### 10.1 运行/停止

- ▶ 运行（注入调试桥接）→ 真实游戏窗口 → 按钮变红 ■ 停止；**2s 轮询状态**：游戏自行退出后按钮自动恢复 ▶；同一项目同时一个实例。

### 10.2 路线图（🗺）

- label 状态机图 + 角色推断（start 起点/ending 结局/choice 选择/loop 循环/orphan 孤立）；点击节点运行中跳游戏；轮询高亮当前节点。

### 10.3 实时画面（🎬）

- 桥接定时截图 + 点击/推进/回滚指令（模拟鼠标点击游戏按钮，聚焦后点击）。

### 10.4 运行时变量（📊）

- 变量快照轮询（2s）+ 变化高亮 1.5s + 点击跳转定义处。

### 10.5 报错诊断（🐞）

- 崩溃文件结构化：While 位置/根因帧（最深 game/ 帧）/完整栈帧/lint 错误/log 内嵌；点击跳转。

### 10.6 静态诊断（✅）

- 五类引用完整性扫描（无效跳转/未定义 screen/角色/缺失资源/不可达 label）；分组着色；点击跳转。

### 操作与测试

运行 → 截图 → 路线图点节点跳游戏 → 实时画面点击 → 变量变化高亮 → 制造崩溃看 🐞 → ✅ 扫描五类。

---

## 11. 与 AI 协作（功能详解）

- **侧栏对话**：消息流（助手消息 **Markdown 渲染**、**🤔 思考 ▸** 展开推理、hover「⧉ 复制」；用户消息 hover「✎ 编辑」改后重发）。
- **轨迹**：对话下方工具调用记录（lint/run/edit/teach 等），**编辑类条目显示「✎ 文件名」点击跳转编辑器**对应位置；运行类显示进行中状态。
- **检查点时间线**：每轮对话一个检查点，点击切到历史版本查看/恢复（§7）。
- **工作区域联动**：设置/清除工作范围会向对话注入约束消息，AI 行为受约束（§8）。
- **学习注释**：📖 批量讲解（消耗 AI 资源，§3.1）。

### 操作与测试

发「把 script.rpy 第一句对白改成‘你好，世界’」→ AI 修改 → 轨迹 edit 条目 → 点击跳转 → 检查点时间线新增。

---

## 12. 个性化设置（⚙ 底部按钮，功能详解）

### 12.1 总览

- **入口**：控件栏底部 ⚙ → **全屏设置面板**（Esc/✕ 关闭）；顶部**搜索**可过滤。
- **分层**：右上角 **全局/项目** chips——全局=所有项目生效，项目=仅当前项目（存在 `.renpy-user/settings/<project>.json`，**不写入项目目录**）。
- **生效**：改动**即时生效**（字号/字体改动会重载编辑器）；「↺ 重置」单项恢复默认；底部标注「配置键名与语义对齐 VSCode（editor.*，MIT 借鉴）」。
- **预制配色**：第一组颜色上方「🎨 应用预制配色」下拉，内置 **8 套主题**（2026 Dark/Light、Dark+/Light+ 等）一键套用。

### 12.2 设置项全表（48 项，按分组）

**功能 · 编辑行为**
| 配置 | 说明 | 默认 |
|---|---|---|
| editor.tabSize | 缩进宽度 | 4 |
| editor.insertSpaces | 用空格缩进 | 开 |
| editor.mouseWheelZoom | 滚轮缩放字号 | 关 |
| editor.smoothScrolling | 平滑滚动 | 开 |
| editor.trimAutoWhitespace | 保存时去除行尾空白 | 开 |
| editor.padding.top / bottom | 编辑器上下内边距 | 0 |

**功能 · 补全**
| 配置 | 说明 | 默认 |
|---|---|---|
| editor.quickSuggestions.other | 输入时自动弹出补全 | 开 |
| editor.quickSuggestions.comments | 注释内也补全 | 关 |
| editor.quickSuggestions.strings | 字符串内补全 | 关 |
| editor.suggestOnTriggerCharacters | 触发字符（如 `{`）自动弹出 | 开 |
| editor.bracketPairColorization.enabled | 括号配对着色 | 开 |
| editor.guides.indentation | 缩进参考线 | 开 |

**功能 · 显示**
| 配置 | 说明 | 默认 |
|---|---|---|
| editor.lineNumbers | 行号模式：on / relative / off | on |
| editor.renderLineHighlight | 当前行高亮范围（none/line/gutter/all） | all |
| editor.renderWhitespace | 空白显示（none/boundary/trailing/all） | boundary |
| editor.rulers | 垂直参考线列号（数组，如 [80]） | 空 |

**功能 · 亮暗模式**
| 配置 | 说明 | 默认 |
|---|---|---|
| theme.mode | 工作台亮暗主题：light / dark | dark |

**控件 · 字体**
| 配置 | 说明 | 默认 |
|---|---|---|
| editor.fontFamily | 编辑器字体（空=跟随主题） | 空 |
| editor.fontSize | 字号 | 13 |
| editor.fontWeight | 字重（400/500/600…） | 400 |
| editor.lineHeight | 行高 px（**0=自动=字号×1.5**） | 0 |
| editor.letterSpacing | 字距 px | 0 |

**控件 · 颜色（12 编辑器 + 8 界面 + 5 交互 = 25 个）**
| 配置 | 说明 |
|---|---|
| editor.background / foreground | 编辑器背景/前景 |
| editor.lineHighlightBackground | 当前行高亮色 |
| editor.selectionBackground | 选区底色 |
| editorIndentGuide.background1 | 缩进参考线色 |
| editorBracketMatch.background | 括号配对高亮 |
| editorFindMatchBackground | 查找高亮 |
| editorError.foreground | 错误下划线色 |
| editorWhitespace.foreground | 空白字符标记色 |
| editorCursor.foreground | 光标色 |
| editorGutter.background | 行号栏底色 |
| editorLineNumber.foreground | 行号色 |
| workbench.background | 工作台底色 |
| workbench.sideBar.background | 侧栏底色 |
| workbench.activityBar.background | 控件栏底色 |
| workbench.panel.background | 面板底色 |
| workbench.editorGroupHeader.tabsBackground | 标签栏底色 |
| workbench.statusBar.background | 状态栏底色 |
| workbench.foreground / border | 工作台前景/边框 |
| button.background / foreground | 按钮底色/前景 |
| input.background / border | 输入框底色/边框 |
| list.hoverBackground | 列表悬停色 |

### 操作与测试

- 改字号 16 → 编辑器立即变大；改行号 relative → 行号变距离显示
- 切 light/dark → 整个工作台明暗切换
- 套「2026 Dark」配色 → 全部颜色变化 → 重置
- 全局/项目 chips：项目层改一处 → 切到另一个项目验证不生效（隔离）

---

## 13. 快捷键表

| 快捷键 | 功能 |
|---|---|
| `Ctrl+S` | 保存当前文件 |
| `Ctrl+F` | 查找/替换 |
| `Ctrl+/` | 注释切换 |
| `Ctrl+Space` | 代码补全 |
| `Ctrl+Shift+\` | 括号跳转 |
| `Enter` | 发送对话消息 |
| `Esc` | 关闭弹层/浮窗/设置面板 |

---

## 14. 完整回归测试清单

> 按顺序逐条执行，全部符合预期即基础功能健康。每条约 1 分钟。测试后按 §16 模板反馈。

| # | 功能 | 操作步骤 | 预期结果 |
|---|---|---|---|
| 1 | 面板打开 | 新会话 → Ren'Py 页签 → 加载项目 | 文件树/编辑器/控件栏/侧栏全部出现 |
| 2 | 文件浏览 | 展开文件树，打开 2 个 .rpy | 多标签页切换正常；点 🧭 导航可跳转 |
| 3 | 资源预览 | 🖼 素材 → 点一张图片、一个音频 | 右下角弹出预览浮窗 |
| 4 | 编辑保存 | 改一行 → Ctrl+S | 状态栏 ✓；未保存提示消失 |
| 5 | 补全 | 输入 `menu` + Ctrl+Space | 语句/片段补全面板出现 |
| 6 | 查找替换 | Ctrl+F → 查找 → 替换一个/全部 | 高亮、导航、替换正确 |
| 7 | lint | ⚠ 检查 | 通过（无错时）；故意弄错 → 红线定位 |
| 8 | 运行/停止 | ▶ 运行 → 等游戏窗口 → 再点 ■ | 窗口弹出、按钮状态切换、停止后恢复 |
| 9 | 截图 | 运行中 📷 截图 | 生成 PNG，对话中可查看 |
| 10 | 测试 | 🧪 测试（demo 项目） | 报告全通过；改坏后出现失败 |
| 11 | 历史回滚 | 保存 3 次 → 📜 → 恢复最早版 | 内容还原 |
| 12 | 检查点 | 让 AI 改代码 → ✎ 修改 | diff 准确、gutter 标记、通过/撤回可用 |
| 13 | 工作范围 | 🎯 锁 5-10 行 | 区域外拦截；AI 只改区域内 |
| 14 | 写守卫 | 写 `label if:` 保存 | 被拦截 + 错误列表；强制可绕过 |
| 15 | 学习注释 | 📖 → 确认 | 注释写入、lint 通过、清除可用 |
| 16 | 样式预览 | Aa → 点击对白行 | 富文本渲染；打字动画浮窗播放 |
| 17 | Python 对照 | ⇄ 停在 `show` 语句 | 浮窗显示 renpy.show(...) |
| 18 | GUI 定制 | 🎨 → 改主题色 → 保存 | gui.rpy 写回 |
| 19 | 静态诊断 | ✅ | 五类检查分组显示；点击跳转 |
| 20 | 报错诊断 | 制造崩溃（运行中改坏脚本）→ 🐞 | traceback 结构化显示、跳转 |
| 21 | Python 高亮 | 打开含 python 块的 .rpy | 关键词紫/类型青/内置蓝/调用黄 |
| 22 | 设置 | ⚙ → 改字号/切亮暗/套配色 | 即时生效；Esc 关闭 |
| 23 | 侧栏对话 | 发消息让 AI 改代码 | Markdown/思考/轨迹跳转/检查点时间线正常 |

---

## 15. 已知限制与注意事项

1. **host 改动需重启 dsh**：涉及后端服务（`host.js`）的更新要完全退出 dsh 再启动；只改界面（client）刷新页面即可。
2. **学习注释消耗 AI 资源**：每次生成调用一次模型（批量一次调用），生成可能需数秒至数十秒（90 秒超时保护）。
3. **工作范围是硬约束**：设置后 AI 修改被限定在区域内，改区域外 AI 会先询问——这是设计行为。
4. **打包运行暂不支持**：面板无发布打包按钮（SDK launcher 内部功能）。
5. **备份/检查点不自动清理**：`.renpy-user/backups/`、`checkpoints/` 会累积，需要时手动清理。
6. **写 `init python: import renpy` 会触发引擎异常**（lint 报错），不要写。
7. **部分操作可能触发 DSH 审批提示**（沙箱策略），允许即可。
8. **运行/停止合一的状态由轮询驱动**：极端情况下（游戏刚启动 2 秒内）按钮可能短暂显示 ▶，属正常。

---

## 16. 常见问题（FAQ）

**Q：加载项目后文件树是空的？**
检查项目路径是否为含 `game/` 的 Ren'Py 项目根目录（如 `...\demo-script`），点 ⟳ 重新加载。

**Q：AI 改的文件我看不到变化？**
编辑器会自动同步外部修改（5s 轮询）。若该文件你有未保存的本地修改，会提示冲突并保留本地——关闭标签重开加载外部版本。

**Q：运行游戏后窗口没出现？**
首次运行可能触发审批（沙箱策略），检查顶栏状态与对话日志；游戏窗口由 SDK 真实打开，与 launcher 一致。

**Q：📖 学习提示「AI 教学失败: ?」？**
多为模型临时限流/空响应，稍后重试；批量行数多时建议先缩小工作区域。

**Q：如何彻底重置测试环境？**
删除 `.renpy-user/` 下的 backups/checkpoints/workspace 子目录（保留目录本身），刷新页面。

**Q：在哪里看每个功能的实现细节？**
开源仓库文档随版本持续更新，记录各功能的实现、验证方式与已知问题（本文档 §1-12 为功能详解）。

---

## 17. 测试结果反馈

测试中发现任何问题（功能不符/界面错位/崩溃/文案错误），请记录：

```
【测试日期】____
【功能】____（如：静态诊断）
【操作步骤】1. ____ 2. ____ 3. ____
【预期】____
【实际】____（可附截图）
【环境】亮/暗主题
```

反馈后开发者据此修复并更新本文档。

---

## 18. 把积累的个人经验交给开发者

> 使用中你会积累大量一手经验：踩过的坑、验证过的写法、适合自己项目的习惯。
> 这套工具按**三层经验隔离**设计（`renpy-practices` skill 顶部有完整声明）。

### 18.1 三层经验是什么

| 层级 | 内容 | 归属 |
|---|---|---|
| **L1 引擎事实** | 语法/语义/API 的确定性结论（经源码核验 + lint 验证） | 进开源包（renpy-* skill） |
| **L2 通用原则** | 组织/命名/性能的"道理"，不绑定具体项目 | 进开源包（renpy-practices） |
| **L3 个人经验** | 你的项目、你的习惯、个人踩坑 | **你的私有文件**，不进开源包 |

优先级：`L1 > L2 > L3 > 模型常识`。

### 18.2 经验先记到你的私有文件（L3）

使用中随时把新经验追加到你的个人文件：

```
~/.dsh/skills/renpy-practices-personal.md
```

- 文件不存在就自己创建（普通 Markdown 即可）。
- 内容随意：踩坑记录、验证过的写法、项目特有约定、对 skill 的疑问。
- 模型写代码时会自动把它作为参考（skill 目录里的文件都会被加载），**你的经验立即生效**。

建议条目格式：

```markdown
## <主题>
- 现象/背景：……
- 实测结果：……
- 结论：……
- 想确认：是否 L1/L2 可进开源包？
```

### 18.3 回传给开发者（两种方式）

**方式 A：直接把个人文件发过去（最简单）**

把你的 `renpy-practices-personal.md` 原样发送给开发者。开发者会自己筛选：L3 保留在你侧，L1/L2 候选进入核验流程。

**方式 B：按模板整理后提交（推荐）**

对每一条想贡献的经验，填这个模板，和文件一起发：

```markdown
- 经验：一句话结论
- 属于哪层（L1 引擎事实 / L2 通用原则 / L3 个人经验）：____
- 复现/依据：怎么得到的？（实测 / 文档 / 源码 / 猜测）
- 是否已用 renpy_lint 验证：是 / 否
- 最小示例（可跑）：……
```

- **L1 条目**：附上验证方式（lint 输出、最小 .rpy 示例、SDK 版本），便于开发者核验。
- **L2 条目**：说明适用场景与理由。
- **L3 条目**：标注"个人经验，无需入库"，开发者不会采用到开源包。

### 18.4 开发者收到后会发生什么

| 你的提交 | 开发者的处理 |
|---|---|
| 问题/Bug 反馈 | 记录并跟进 → 修复 → 回归测试确认 |
| L3 个人经验 | 保留在你侧，不进开源包（可能给通用化建议） |
| L2 通用原则 | 核验"道理"成立 → 并入 renpy-practices 对应节 |
| L1 引擎事实 | 源码核验 + lint 验证 → 并入对应 renpy-* skill（会标注来源） |

> 一旦 L1/L2 经验进了开源包，所有用这套工具的人（和他们的 AI）都会受益——这是经验回传的最大价值。个人文件里的 L3 部分始终属于你，随时可改可删。
