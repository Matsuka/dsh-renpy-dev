# Ren'Py 开发模式 — 测试者指南

> 面向测试人员的功能介绍与操作手册。本文档说明这套工具能做什么、怎么打开、每一步怎么操作、预期看到什么结果，以及测试时需要注意的事项。
>
> 配套阅读：**部署流程**见 `DEPLOY.md`，**贡献指南**见 `CONTRIBUTING.md`。

---

## 1. 这是什么

Ren'Py 开发模式是一套装在 **DeepSeek Harness（DSH）** 里的 **Ren'Py 游戏开发工作台**。它把一个普通的 AI 编程对话界面，扩展成了带完整开发工具链的浏览器面板：

- 左边是**文件/资源管理器**，中间是**代码编辑器**（语法高亮、补全、查找替换、括号匹配等，类 VSCode）
- 顶部工具栏可以**检查（lint）、运行游戏、截图、跑自动化测试**
- 每次保存自动**备份历史**、每次对话自动建立**检查点**，随时可以回滚
- 可以和 AI 对话，让 AI 帮你改代码，AI 改完你能在编辑器里**实时看到改动**并审阅
- 内置 **14 个 Ren'Py 知识库（skill）**，AI 写 Ren'Py 代码时会按需加载，减少瞎编语法

一句话：**在浏览器里完成"看代码 → 改代码 → 验证 → 运行"的整个 Ren'Py 开发循环，AI 全程参与但你能看到每一步。**

---

## 2. 环境与启动

### 2.1 前置条件（已配置好）

| 项目 | 位置/说明 |
|---|---|
| DSH | 本机已安装并运行（Web GUI `http://127.0.0.1:3080`） |
| Ren'Py SDK | 工作区 `renpy-8.5.3-sdk/`（锁定 8.5.x） |
| Ren'Py 开发模式 | 已挂载为 agent preset「RenPy Dev」，新会话可选 |
| 示例项目 | `demo-script/`（测试用）；SDK 自带 `the_question/`、`tutorial/` |

### 2.2 打开开发面板

1. 在 DSH Web GUI 开一个新会话（或切换 preset 到 **RenPy Dev**）
2. 在会话的对话页签上方找到 **"Ren'Py"** 页签并点击 → 出现开发面板
3. 顶部输入框输入你的 Ren'Py 项目路径（含 `game/` 目录的项目根，如 `D:\my-game`），点 **⟳ 加载**

> 加载后左侧出现项目文件树，右侧出现编辑器，面板开始工作。

### 2.3 生效规则（重要）

- **只改了编辑器 UI（client 侧）** → 浏览器**刷新页面**即生效
- **改了后端服务（host 侧）** → 需要**重启 dsh** 才生效（文档中凡是提到 host 改动的功能都如此）
- **改了 agent preset / skill** → 重启 dsh 后**新会话**生效

---

## 3. 界面导览

```
┌─────────────┬──────────────────────────────────────┬─────────────┐
│  活动栏      │  顶栏工具栏                          │             │
│  📄 文件     │  🔒工作范围 ⟳加载 ⚠检查 🧪测试      │   侧栏对话   │
│  🖼 资源     │  📖学习 ▶运行 ■停止 📷截图          │  （消息流 +  │
│  ✎ 修改     │  💾保存 🕘历史 🎨GUI ⇄Python Aa预览  │   检查点时间线│
├─────────────┼──────────────────────────────────────┤  + 轨迹）    │
│ 文件树/      │  编辑器（多文件标签页）               │             │
│ 资源树/      │  查找栏 / 错误下划线 / 工作区高亮     │             │
│ 修改列表     │                                      │             │
│             ├──────────────────────────────────────┤             │
│             │  未保存修改提示条 / lint 结果 /        │             │
│             │  test 报告                            │             │
├─────────────┴──────────────────────────────────────┴─────────────┤
│  状态栏：文件名 | 行:列 | ●未保存/✓已保存 | .rpy                    │
└────────────────────────────────────────────────────────────────────┘
```

**快捷键**：`Ctrl+S` 保存 ｜ `Ctrl+F` 查找/替换 ｜ `Ctrl+/` 注释切换 ｜ `Ctrl+Space` 代码补全 ｜ `Ctrl+Shift+\` 括号跳转 ｜ `Enter` 发送对话

---

## 4. 核心功能使用说明

> 每个功能按「怎么操作 → 预期结果 → 测试要点」说明。★ 表示建议优先测试的重点功能。

### 4.1 浏览项目与资源

- **操作**：加载项目后，左侧上方是**文件树**（📄，按目录展开，点击打开），下方是**资源树**（🖼 图片/🎵 音频/🎬 视频/🔤 字体/📦 其他，点击叶子可预览）
- **预期**：文件树列出 `game/` 下所有 .rpy；资源树按分类展示，图片/音频点开右下角弹出预览浮窗（可拖动）
- **测试要点**：打开 2-3 个文件形成多标签页；预览一张图片和一个音频；刷新（⟳）后新文件出现

### 4.2 编辑代码 ★

- **操作**：点击文件树中某个 .rpy → 编辑器打开 → 直接输入修改 → `Ctrl+S` 保存
- **预期**：输入语句时自动弹出**补全**（语句/角色/资源/代码片段）；括号自动配对；`Ctrl+/` 注释；保存后状态栏变 ✓
- **测试要点**：
  - 输入 `menu`、`if`、`label` 看补全片段
  - `Ctrl+F` 查找，验证匹配高亮、`Enter/Shift+Enter` 导航、替换单个/全部
  - 修改后不保存 → 编辑器下方出现「📝 未保存修改 +N 行 -M 行 [保存] [撤回修改]」
  - 不保存时让 AI 改同一个文件 → 出现「⚠ 外部修改冲突」提示（本地修改保留），关闭标签重开可加载外部版本

### 4.3 语法检查（lint）★

- **操作**：点顶栏 **⚠ 检查**
- **预期**：下方 lint 面板显示结果；**有错误的行在编辑器内出现红色下划线**，点击 lint 条目可跳转
- **测试要点**：故意删掉一个语句的冒号或引号 → 检查 → 应报错并下划线定位；修复后复查应为 0 错误

### 4.4 运行游戏与截图 ★

- **操作**：点 **▶ 运行**（真实打开游戏窗口）→ 玩到想看的位置 → 点 **📷 截图** → 点 **■ 停止**
- **预期**：游戏窗口弹出；截图保存后可在面板/对话中查看（AI 也能通过截图看到画面）
- **测试要点**：运行后点截图，确认生成了当前画面的 PNG；停止后进程关闭（顶栏状态回位）；截图路径在 `.renpy-user/screenshots/`

### 4.5 自动化测试

- **操作**：项目里有 `testsuite` 时点 **🧪 测试**
- **预期**：运行 rpytest，报告条显示 ✓/✗ 与通过数，「详情」可展开完整报告
- **测试要点**：demo-script 自带 2 个用例应全部通过；改坏一处流程后应出现失败用例

### 4.6 保存历史与回滚 ★

- **操作**：编辑并保存 → 点 **🕘 历史**
- **预期**：弹层列出该文件的历史版本（每次保存自动备份，时间倒序），点某版本右侧预览内容，点「恢复」一键还原
- **测试要点**：保存 2-3 次后打开历史，恢复最早版本，确认文件内容还原；备份目录在 `.renpy-user/backups/`

### 4.7 检查点与修改面板 ★

- **操作**：正常对话让 AI 改代码（或手动保存）→ 顶栏 **✎ 修改 (N)** 按钮出现改动数徽标 → 点击打开修改面板
- **预期**：
  - 面板按文件列出 `+A -B` 行级 diff，展开 hunk 显示起始行，点击可跳转编辑器
  - 编辑器 gutter 有颜色标记（绿=新增 蓝=修改 红=删除）
  - 可对单个/全部文件「通过」或「撤回」；侧栏对话底部有「📌 持久检查点」时间线，可切到任意历史检查点查看/恢复
- **测试要点**：让 AI 改几行代码 → 看修改面板 diff 是否准确 → 「撤回」→ 文件还原 → 「通过」→ 基线推进；检查点时间线出现新条目

### 4.8 工作区域锁定 ★

- **操作**：编辑器里选中几行（或光标停在某行）→ 点 **🔒 工作范围** → 出现绿色区域高亮；点旁边的「✖ 清除」解除
- **预期**：区域外**编辑被拦截**（输入无效并提示）；区域设置/解除会向对话注入【工作区域】约束消息，AI 的修改会限定在区域内
- **测试要点**：
  - 设 8-15 行，尝试在区域外打字 → 被拒
  - 让 AI 改文件 → 确认它只动区域内代码（persona 有"越界先询问"规则）
  - 解除锁定后 AI 可自由修改

### 4.9 学习注释（AI 教学）★

- **操作**：打开一个 .rpy → 点 **📖 学习** → 弹确认框（显示范围：工作区域内 Lx-y / 整个文件 + 行数 + 消耗提示）→ 确认
- **预期**：AI 为范围内语句行生成 `# 📖 学习: <skill>（L<行>）` 注释块写入文件（讲解"这段代码在做什么"）；结果条显示「已写入 N 条」+「🗑 清除全部」
- **测试要点**：
  - 确认弹窗文案与范围正确；生成后文件 lint 应通过（注释合法）
  - 「清除全部」后注释消失
  - 工作区域锁定下点📖 → 只注释区域内行
  - ⚠ 该功能**消耗 AI 资源**（调用模型），连续大量行可能较慢

### 4.10 文本样式预览与打字动画

- **操作**：编辑器打开含 `say` 文本的文件 → 点 **Aa 预览**（再点关闭）
- **预期**：say 文本**直接以样式渲染**（粗/斜/下划线/删除线/颜色/透明/字距等真实显示；字号/字体/插值等以黄/蓝/红底色标记 + 悬停提示）；行高自动放大
- **打字动画**：预览模式下**点击 say 行** → 右下角浮窗逐字播放（速度读项目配置，如 `style say_dialogue: slow_cps 30`），▶ 重播、✕ 关闭
- **测试要点**：含 `{b}` `{color}` `{size}` `{font}` `{cps}` 的文本行分别验证；降级提示条（⚠ 样式预览降级提示）在无法精确渲染时出现

### 4.11 GUI 主题定制

- **操作**：打开 `gui.rpy` 项目 → 点 **🎨 GUI**
- **预期**：面板显示分辨率（gui.init）、7 个主题色（颜色选择器）、字号等；修改后「保存到 gui.rpy」写回文件，编辑器标签同步刷新；「查看源码」直接打开 gui.rpy
- **测试要点**：改一个主题色保存 → 文件里对应 `gui.xxx_color` 变化；无 gui.rpy 的项目打开显示默认值，保存会新建

### 4.12 语句 ⇄ Python 等价

- **操作**：光标停在某条 Ren'Py 语句（如 `show eileen happy at right`）→ 点 **⇄ Python**
- **预期**：右下角浮窗显示该语句的 Python 等价（如 `renpy.show(...)`）+ 语义说明；无法识别则日志提示
- **测试要点**：分别试 `say / jump / call / scene / with / define / if / menu` 等

### 4.13 侧栏对话（面板内置聊天）

- **操作**：面板右侧聊天框输入消息发送（或用 DSH 原生输入框）
- **预期**：消息流展示；助手消息支持 **Markdown 渲染**（代码块/粗体/列表）、**🤔 思考 ▸** 可展开推理过程、hover 显示「⧉ 复制」、用户消息 hover 显示「✎ 编辑」可改后重发；下方有**轨迹**（工具调用记录，编辑类显示「✎ 文件名」点击可跳转编辑器对应位置）与**检查点时间线**
- **测试要点**：发一条让 AI 改代码的消息 → 轨迹出现 edit 条目 → 点击跳转到编辑器；改完后检查点时间线新增条目

---

## 5. 测试清单（快速回归表）

> 按顺序过一遍，全部通过即核心功能健康。

| # | 功能 | 操作 | 预期 |
|---|---|---|---|
| 1 | 面板打开 | 新会话 → Ren'Py 页签 → 加载项目 | 文件树/编辑器/侧栏出现 |
| 2 | 编辑+保存 | 改文件 → Ctrl+S | 状态栏 ✓；未保存提示消失 |
| 3 | lint | ⚠ 检查 | 无报错；有错时下划线定位 |
| 4 | 补全/查找 | 输入关键字 / Ctrl+F | 补全面板、查找高亮正常 |
| 5 | 运行/截图/停止 | ▶ → 📷 → ■ | 窗口弹出、截图生成、进程停止 |
| 6 | 自动化测试 | 🧪 测试 | 报告通过/失败正确 |
| 7 | 保存历史 | 多次保存 → 🕘 | 版本列表、预览、恢复可用 |
| 8 | 检查点 | 让 AI 改代码 → ✎ | diff 准确、gutter 标记、通过/撤回可用 |
| 9 | 工作区域 | 🔒 锁 8-15 行 | 区域外拦截；对话注入约束 |
| 10 | 学习注释 | 📖 → 确认 | 注释写入、lint 通过、清除可用 |
| 11 | 样式预览 | Aa 预览 | 样式所见即所得；点击播放打字动画 |
| 12 | GUI 定制 | 🎨 → 改色 → 保存 | gui.rpy 写回 |
| 13 | 语句⇄Python | ⇄ Python | 等价转换正确 |
| 14 | 侧栏对话 | 发消息 | Markdown/思考/编辑重发/轨迹跳转正常 |

---

## 6. 已知限制与注意事项

1. **打包运行暂不支持**：Ren'Py 的打包（distribute）在 launcher 内部，SDK 命令行无法直接调用 → 面板无打包按钮，`renpy-build` 技能只覆盖 build.rpy 配置知识。
2. **host 改动需重启 dsh**：任何涉及 `renpy-client/lib/host.js` 的功能更新，重启才生效；client 侧改动刷新页面即可。
3. **学习注释消耗 AI 资源**：每次生成调用一次模型（批量一次调用），会消耗 token；生成可能需数秒至数十秒（90s 超时保护）。
4. **工作区域是硬约束**：设置后 AI 修改被限定在区域内，区域外代码不动；需要改区域外时 AI 会先询问——这是设计行为，不是 bug。
5. **沙箱/审批**：部分操作（如首次写特定目录）可能触发 DSH 审批提示，允许即可；运行游戏等涉及进程的操作走已配置的沙箱策略。
6. **备份/检查点不自动清理**：`.renpy-user/backups/` 与 `checkpoints/` 会持续累积，需要时手动清理。
7. **`init python: import renpy` 不要写**：会触发引擎初始化异常（lint 报 `renpy.music` 属性缺失）；`game/cache` 损坏时清空该目录可恢复。
8. **旧会话历史损坏不可恢复**：若某会话出现模型 400 报错（工具消息配对错误），直接开新会话即可（该 bug 已在 §21x-8 修复）。

---

## 7. 常见问题（FAQ）

**Q：加载项目后文件树是空的？**
检查项目路径是否为含 `game/` 的 Ren'Py 项目根目录（如 `...\demo-script`），点 ⟳ 重新加载。

**Q：AI 改的文件我看不到变化？**
编辑器会自动同步外部修改（5s 轮询）。若该文件你有未保存的本地修改，会提示冲突并保留本地——关闭标签重开加载外部版本。

**Q：运行游戏后窗口没出现？**
首次运行可能触发审批；检查顶栏状态与对话日志。游戏窗口由 SDK 真实打开，与 launcher 一致。

**Q：📖 学习提示"AI 教学失败: ?"？**
多为模型临时限流/空响应，稍后重试；批量行数多时建议先缩小工作区域。

**Q：如何彻底重置测试环境？**
删除 `.renpy-user/` 下的 backups/checkpoints/workspace 子目录（保留目录本身），刷新页面。

**Q：在哪里看每个功能的实现细节？**
开源仓库文档会随版本持续更新，记录各功能的实现、验证方式与已知问题。

---

## 8. 反馈

测试中发现任何问题，请记录：**功能 → 操作步骤 → 实际表现 → 期望表现**，最好附截图/日志。问题修复后会在文档与回归测试中确认。

---

## 9. 把积累的个人经验交给开发者

> 使用中你会积累大量一手经验：踩过的坑、验证过的写法、适合自己项目的习惯。
> 这套工具按**三层经验隔离**设计（`renpy-practices` skill 顶部有完整声明），
> 本节省略了"如何把经验沉淀下来并回传给开发者"。

### 9.1 三层经验是什么

| 层级 | 内容 | 归属 |
|---|---|---|
| **L1 引擎事实** | 语法/语义/API 的确定性结论（经源码核验 + lint 验证） | 进开源包（renpy-* skill） |
| **L2 通用原则** | 组织/命名/性能的"道理"，不绑定具体项目 | 进开源包（renpy-practices） |
| **L3 个人经验** | 你的项目、你的习惯、个人踩坑 | **你的私有文件**，不进开源包 |

优先级：`L1 > L2 > L3 > 模型常识`。

### 9.2 经验先记到你的私有文件（L3）

使用中随时把新经验追加到你的个人文件：

```
~/.dsh/skills/renpy-practices-personal.md
```

- 文件不存在就自己创建（普通 Markdown 即可）。
- 内容随意：踩坑记录、验证过的写法、项目特有约定、对 skill 的疑问。
- 模型写代码时会自动把它作为参考（skill 目录里的文件都会被加载），**你的经验立即生效**——这是第一层价值。

建议条目格式：

```markdown
## <主题>
- 现象/背景：……
- 实测结果：……
- 结论：……
- 想确认：是否 L1/L2 可进开源包？
```

### 9.3 整理回传的时机

- 每完成一轮测试（或每攒 5-10 条）回传一次，避免堆积。
- 回传前做一次**分级**：这条是引擎事实（L1）？通用原则（L2）？还是只有你自己适用（L3）？

### 9.4 回传给开发者（三种方式，按能力选）

**方式 A：直接把个人文件发过去（最简单）**

把你的 `renpy-practices-personal.md` 原样发送给开发者（邮件/IM/网盘均可）。
开发者会自己筛选：L3 保留在你侧，L1/L2 候选进入核验流程。

**方式 B：按模板整理后提交（推荐）**

对每一条想贡献的经验，填这个模板，和文件一起发：

```markdown
- 经验：一句话结论
- 属于哪层（L1 引擎事实 / L2 通用原则 / L3 个人经验）：____
- 复现/依据：怎么得到的？（实测 / 文档 / 源码 / 猜测）
- 是否已用 renpy_lint 验证：是 / 否
- 最小示例（可跑）：……
```

- **L1 条目**：附上验证方式（lint 输出、最小 .rpy 示例、SDK 版本），便于开发者核验。
- **L2 条目**：说明适用场景与理由。
- **L3 条目**：标注"个人经验，无需入库"，开发者不会采用到开源包。

**方式 C：参与开源维护（长期）**

若项目开源，可按贡献指南直接提交（PR）：
- L1/L2 条目经**源码核验 + lint 验证**后并入对应 renpy-* skill；
- L3 条目留在你的私有文件，不进开源包；
- 核验标准与提交格式见 `renpy-practices` skill 的"贡献指南"节。

### 9.5 开发者收到后会发生什么

| 你的提交 | 开发者的处理 |
|---|---|
| 问题/Bug 反馈 | 记录并跟进 → 修复 → 回归测试确认 |
| L3 个人经验 | 保留在你侧，不进开源包（可能给通用化建议） |
| L2 通用原则 | 核验"道理"成立 → 并入 renpy-practices 对应节 |
| L1 引擎事实 | 源码核验 + lint 验证 → 并入对应 renpy-* skill（会标注来源） |

> 一旦 L1/L2 经验进了开源包，所有用这套工具的人（和他们的 AI）都会受益——这是经验回传的最大价值。个人文件里的 L3 部分始终属于你，随时可改可删。
